# V2: Mamba-3+Attention hybrid predictor
# Base dependencies from LeWM paper
stable-pretraining[train]
stable-worldmodel
huggingface_hub
hydra-core
einops

# Mamba-3 (CUDA required for training — Colab/Vast only)
# Install via: pip install mamba-ssm
# Or from source: git clone https://github.com/state-spaces/mamba && cd mamba && pip install .
mamba-ssm
